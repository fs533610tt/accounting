# 桌球隊作帳系統 - 開發實作計畫

本計畫旨在建立一個獨立的球隊作帳系統，前端採用 React 並部署至 GitHub Pages，後端採用 Supabase 免費方案（包含身份驗證與 PostgreSQL 資料庫）。

> [!NOTE]
> 考量到此系統「可能會有多人使用」，我們在資料庫設計上會導入「多租戶 (Multi-tenant)」或「角色權限 (RBAC)」的概念，讓不同球隊或不同管理者可以各自登入，且資料互不干擾。

## User Review Required

> [!IMPORTANT]
> 由於 GitHub Pages 是純靜態網頁託管，為避免重新整理時發生 404 錯誤，前端路由建議採用 `HashRouter` (網址會有個 `#`)。這會是最穩定且無需額外後端設定的做法。

## Open Questions

> [!WARNING]
> **設計風格**：系統會採用極致現代化、高質感的 UI 設計 (Glassmorphism, 漸層, 微動畫)。您是否有偏好的主色系 (例如科技藍、活力橘)？

---

## Proposed Changes

### 1. 系統架構與業務流程更新

- **前端框架**：Vite + React (JavaScript/JSX)
- **部署環境**：GitHub Pages
- **後端與資料庫**：Supabase
- **登入機制 (新增)**：整合 **Google 登入 (Google OAuth)**。

#### A. 角色與權限分配 (RBAC)
為了確保系統安全與操作簡便，**本系統定位為球隊內部管理工具，家長不需登入**。會登入系統的使用者分為以下三種角色：
1. **幹部 / 系統管理員 (Admin)**：擁有所有權限，可建立球隊、產生邀請碼、管理教練資料與時薪設定。
2. **會計人員 (Accountant)**：負責確認學生繳費 (核銷與列印學費袋)、結算並發放教練薪水、產生與發佈/匯出財務報表。
3. **教練 (Coach)**：只能登入填寫自己的上課時數，上傳存摺，以及查看自己的薪資單。

#### B. 核心業務流程
1. **小朋友收費管理**：建立名單後，月底自動產生帳單，由會計核銷「實收金額」與「匯後五碼」。
   - **學費袋套印功能 (新增)**：針對以現金或紙本作業的需求，系統支援將帳單資訊（姓名、月份、金額等）直接套版列印至現有的公版學費袋（尺寸約 10x21 公分），大幅減輕人工手寫的負擔。
2. **教練薪資與時數追蹤 (新增)**：
   - 管理員設定每位教練的「時薪」。
   - 教練登入後填寫「上課日期、時數」，並可**上傳與更新個人「存摺照片」與「匯款帳號資訊」**。
   - 月底由會計人員點擊「結算薪水」，系統自動計算 `時薪 * 時數`，並將這筆費用匯入「日常流水帳 (支出)」。
   - **會計人員發放薪資時，系統直接提供該教練的匯款帳號與存摺影本供核對，甚至可匯出「銀行批次轉帳檔」，省去人工找帳號與轉帳的時間。**
3. **財務報表發布 (新增)**：
   - 結合小朋友的學費收入、教練的薪水支出與日常零星開銷，系統自動產生「收支月報表」。
   - 提供「發布/匯出 (PDF/截圖)」功能，讓會計可以將精美的財報分享給家長或其他股東看。

### 2. 資料庫結構 (Supabase Database Schema)

為了支援上述流程，更新資料表結構：

#### A. 核心實體與權限
1. **`teams` (球隊表)**
2. **`user_roles` (管理者權限表)**
   - `role` (角色：admin, accountant, coach)
3. **`students` (小朋友/隊員名單)**
   - `id`, `name`, `grade`, `class_name`, `is_officer`

#### B. 帳務與薪資紀錄表 (新增)
4. **`billing_records` (小朋友的每月帳單)**
5. **`ledger_entries` (日常流水帳)**
6. **`coach_timesheets` (教練時數表 - 新增)**
   - `id` (UUID, PK)
   - `team_id` (FK to teams)
   - `coach_user_id` (FK to auth.users)
   - `work_date` (上課日期)
   - `hours` (時數)
   - `status` (狀態：pending, approved, paid)
7. **`coach_rates` (教練時薪設定檔 - 新增)**
   - `team_id`, `coach_user_id`, `hourly_rate` (時薪)
8. **`coach_bank_accounts` (教練匯款帳戶 - 新增)**
   - `id` (UUID, PK)
   - `coach_user_id` (FK to auth.users, Unique)
   - `bank_code` (銀行代碼)
   - `branch_code` (分行代碼)
   - `account_number` (帳號)
   - `account_name` (戶名)
   - `passbook_image_url` (存摺影本照片路徑，存放於 Supabase Storage)

### 3. 開發階段規劃

- **第一階段：專案初始化與 Google 登入**
  - 建立 React 專案與 Supabase 專案。
  - 實作「Google 登入」與「建立/加入球隊」的流程，並加上角色權限 (RBAC) 區隔。
- **第二階段：小朋友名冊與收費管理**
  - 實作小朋友名冊與自動產生帳單的功能。
  - **實作「學費袋套印」功能，利用網頁 Print CSS 精準定位，適配 10x21 公分的實體信封。**
- **第三階段：教練時數與薪資系統 (新增)**
  - 實作教練專屬介面：打卡/填寫時數、**上傳個人存摺照片與設定匯款帳號**。
  - 實作會計介面：設定教練時薪、核准時數並轉為薪資支出、**查看薪資發放對象的匯款資訊**。
- **第四階段：流水帳與自動化報表**
  - 整合所有收支資料，產出「收支月報表」。
  - 實作財報「匯出/發布」功能，部署至 GitHub Pages。

---

## Verification Plan

### 手動驗證
- **權限驗證**：教練帳號登入後，**不應該**看到其他人的薪水或所有學生的繳費紀錄，只能填寫自己的時數。
- **薪資計算驗證**：會計核准教練時數後，系統應自動將該筆薪資認列為流水帳的支出，並正確扣除當月結餘。
